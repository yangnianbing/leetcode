/*
 * @lc app=leetcode id=389 lang=javascript
 *
 * [389] Find the Difference
 *
 * https://leetcode.com/problems/find-the-difference/description/
 *
 * algorithms
 * Easy (52.84%)
 * Total Accepted:    140.5K
 * Total Submissions: 265.6K
 * Testcase Example:  '"abcd"\n"abcde"'
 *
 * 
 * Given two strings s and t which consist of only lowercase letters.
 * 
 * String t is generated by random shuffling string s and then add one more
 * letter at a random position.
 * 
 * Find the letter that was added in t.
 * 
 * Example:
 * 
 * Input:
 * s = "abcd"
 * t = "abcde"
 * 
 * Output:
 * e
 * 
 * Explanation:
 * 'e' is the letter that was added.
 * 
 */
/**
 * @param {string} s
 * @param {string} t
 * @return {character}
 */
var findTheDifference = function(s, t) {
    if(s.length === 0){
        return t;
    }
    let chars = 'abcdefghijklmnopqrstuvwxyz';

    let sSum = s.split('').map(a => a.charCodeAt(0)).reduce((a, b) => a + b);
    let tSum = t.split('').map(a => a.charCodeAt(0)).reduce((a, b) => a + b);

    return chars[tSum - sSum - 'a'.charCodeAt(0)];
};

